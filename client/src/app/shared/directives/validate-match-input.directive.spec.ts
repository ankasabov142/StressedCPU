import { ValidateMatchInputDirective } from './validate-match-input.directive';

describe('ValidateMatchInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateMatchInputDirective();
    expect(directive).toBeTruthy();
  });
});
