import { ValidateUrlDirective } from './validate-url.directive';

describe('ValidateUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateUrlDirective();
    expect(directive).toBeTruthy();
  });
});
